# ============================================
# Backend Environment Variables - Orbitamos
# ============================================
# Copie este arquivo para .env (na raiz do projeto) - USE SÓ EM DESENVOLVIMENTO LOCAL.
# IMPORTANTE: NUNCA commite o arquivo .env (já está no .gitignore).
# No RENDER: configure tudo em Dashboard → orbitamos-backend → Environment (veja docs/RENDER_SUPABASE_SETUP.md).
# Nunca coloque credenciais reais do Supabase aqui; valores abaixo são só para banco local.

# ============================================
# Banco de Dados (Supabase/PostgreSQL)
# ============================================
# OPÇÃO A - Supabase (use quando for usar banco na nuvem):
#   Pegue em: Supabase Dashboard → Settings → Database → Connection string (URI)
#   Formato JDBC: jdbc:postgresql://db.XXXXX.supabase.co:5432/postgres
#   Supabase exige SSL: adicione ?sslmode=require no final da URL
#   Exemplo: jdbc:postgresql://db.abcdefgh.supabase.co:5432/postgres?sslmode=require
# OPÇÃO B - Banco local (Docker/postgres na sua máquina):
#   Com Docker Compose SEM serviço postgres: use localhost só se o postgres rodar na sua máquina.
#   Com postgres em outro container: use o nome do serviço, ex. jdbc:postgresql://db:5432/orbitamos
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/orbitamos
SPRING_DATASOURCE_USERNAME=orbitamos
SPRING_DATASOURCE_PASSWORD=orbitamos123

# ============================================
# JWT (Autenticação) - OBRIGATÓRIO em produção
# ============================================
# Gere uma chave forte de pelo menos 32 caracteres
# Exemplo de geraÃ§Ã£o: openssl rand -base64 32
# Não deixe vazio em produção - configure no Render. Em dev pode ficar vazio (API usa valor default).
JWT_SECRET=

# ExpiraÃ§Ã£o do token em milissegundos (padrÃ£o: 24 horas = 86400000)
JWT_EXPIRATION=86400000

# ============================================
# CORS (Origins permitidos - opcional)
# ============================================
# URLs separadas por vi­rgula (opcional, já configurado no application.yml)
# SPRING_WEB_CORS_ALLOWED_ORIGINS=https://meu-site.com,https://outro-site.com
