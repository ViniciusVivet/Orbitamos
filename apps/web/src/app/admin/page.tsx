"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { BriefcaseBusiness, ClipboardCheck, FolderKanban, Plus, ShieldCheck } from "lucide-react";
import { listAdminApplications, listAdminJobs, listAdminProjects } from "@/lib/workspace";
export default function AdminHome(){const[stats,setStats]=useState({jobs:0,applications:0,review:0,projects:0});useEffect(()=>{Promise.all([listAdminJobs(),listAdminApplications(),listAdminProjects()]).then(([j,a,p])=>setStats({jobs:j.filter(x=>x.status==="open").length,applications:a.length,review:a.filter(x=>x.status==="pending"||x.status==="reviewing").length,projects:p.filter(x=>x.status!=="completed").length}));},[]);const cards=[["Vagas abertas",stats.jobs,BriefcaseBusiness,"/admin/vagas"],["Candidaturas",stats.applications,ClipboardCheck,"/admin/candidaturas"],["Aguardando análise",stats.review,ShieldCheck,"/admin/candidaturas"],["Projetos ativos",stats.projects,FolderKanban,"/admin/projetos"]] as const;return <div className="space-y-7"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-orbit-electric/70">Operação</p><h1 className="mt-1 text-3xl font-black">Visão geral</h1><p className="mt-1 text-sm text-white/40">Publique oportunidades, selecione pessoas e organize entregas.</p></div><Link href="/admin/vagas" className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-orbit-electric px-5 text-xs font-bold text-black"><Plus className="size-4"/>Nova vaga</Link></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,Icon,href])=><Link key={label} href={href} className="rounded-2xl border border-white/10 bg-white/[0.025] p-5 transition hover:-translate-y-0.5 hover:border-orbit-electric/25"><div className="flex items-center justify-between"><span className="text-3xl font-black">{value}</span><div className="grid size-10 place-items-center rounded-xl bg-orbit-electric/10"><Icon className="size-5 text-orbit-electric"/></div></div><p className="mt-2 text-xs text-white/40">{label}</p></Link>)}</div><div className="rounded-2xl border border-white/10 bg-white/[0.02] p-6"><h2 className="font-bold">Fluxo recomendado</h2><div className="mt-4 grid gap-3 md:grid-cols-3">{[["1","Publique uma vaga","Defina escopo, requisitos e prazo."],["2","Avalie candidaturas","Registre status e feedback para cada pessoa."],["3","Monte o projeto","Crie o projeto e adicione o squad selecionado."]].map(([n,t,d])=><div key={n} className="rounded-xl border border-white/8 bg-black/20 p-4"><span className="grid size-7 place-items-center rounded-full bg-orbit-purple/15 text-xs font-black text-orbit-purple">{n}</span><h3 className="mt-3 text-sm font-bold">{t}</h3><p className="mt-1 text-xs leading-5 text-white/35">{d}</p></div>)}</div></div></div>}
